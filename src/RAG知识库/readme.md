# RAG知识库

这是知识库处理实验区，保留了原始文件名，便于按照学习记录查找：

- `固定字符切块.py`：固定长度切分。
- `递归分块.py`：递归字符切分。
- `按标准结构分块-Markdown.py`：按 Markdown 标题层级切分。
- `语义分块.py`：语义切分。
- `检索优化.py`：组合检索与重排实验。
- `load1.py`：读取知识库实验所需环境变量。

这些脚本更适合作为独立实验运行，不建议直接当作生产模块引用。
