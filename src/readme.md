# src 目录

这里存放所有 Python 源码。源码按用途分为四组：

- `agent/`：通用 Agent 基础设施、工具、记忆和数据库实验。
- `RAG开发应用项目/`：健康档案 PDF 的 RAG 主流程。
- `RAG开发应用项目_Agent/`：把 RAG 问答能力包装成 LangGraph Agent。
- `RAG知识库/`：文本切分、向量化和检索策略实验。

根目录中的 `main.py`、`流式输出.py`、`结构化输出1.py`、`结构化输出2.py` 是独立学习示例。
